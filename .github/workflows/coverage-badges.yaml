name: tests + coverage

on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9

      - name: Install dependencies
        run: uv sync

      - name: Run tests (with coverage + badges output)
        run: |
          mkdir -p badges
          uv run pytest

      - name: Upload HTML coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: htmlcov
          path: htmlcov
          if-no-files-found: error

      - name: Upload badges artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: badges
          path: badges
          if-no-files-found: error

      - name: Publish badges to branch "badges"
        # avoid publish loop on PRs
        if: github.event_name == 'push'
        env:
          TARGET_BRANCH: badges
          FILES: |
            tests.svg
            skipped.svg
            coverage.svg
            last-run.svg
            argsui-requires-python.svg
            argsui-version.svg
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          echo ' --- Initial'
          git status
          echo ' --- After Switch'
          git switch -C "${TARGET_BRANCH}"
          git status
          echo ' --- After stashing'
          git stash push
          git status
          echo ' --- After pulling main'
          git pull origin main
          git status
          echo ' --- After poping'
          git stash pop
          git status


          echo ' --- After adding'
          git add -A -f badges
          git status

          if git diff --cached --quiet; then
            echo "No badge changes to publish."
            exit 0
          fi

          echo ' --- After commit'
          git commit -m "Update badges"
          git status
          echo ' >>>'
          git push origin "${TARGET_BRANCH}"
